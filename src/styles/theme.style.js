const deviceSizes = {
  mobile: 550,
};

const DEVICE = {
  mobile: `screen and (max-width: ${deviceSizes.mobile}px)`,
};

const COLORS = {
  white: "#FFFFFF",
  black: "#000000",
};

const theme = {
  COLORS,
  DEVICE,
};

export default theme;
